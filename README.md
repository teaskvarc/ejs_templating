# ejs_templating

### Installation

`$ git clone https://github.com/teaskvarc/ejs_templating.git`

`$ npm install`

`$ bower install`

### Running

`$ node run`

or

`$ supervisor run`

### Overview

The server will run on port `3030`
